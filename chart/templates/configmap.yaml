apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "git-provider.fullname" . }}
  labels:
    {{- include "git-provider.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}